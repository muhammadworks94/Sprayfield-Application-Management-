@model SAM.ViewModels.SystemAdmin.NozzleCreateViewModel
@{
    ViewData["Title"] = "Create Nozzle";
}

<div class="mb-4">
    <p class="text-muted mb-0">Add a new nozzle to the system</p>
</div>

<div class="card">
    <div class="card-body">
        <form asp-action="NozzleCreate" method="post">
            @Html.AntiForgeryToken()

            @await Html.PartialAsync("Partials/_NozzleCreateFormPartial", Model)

            <div class="d-flex justify-content-end gap-2">
                <a asp-action="SystemAdmin" asp-route-tab="nozzles" asp-route-companyId="@Model.CompanyId" class="btn btn-secondary">Cancel</a>
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-check-circle"></i> Create Nozzle
                </button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

